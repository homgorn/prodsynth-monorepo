{
  "name": "@prodsynth/tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "vitest": "^1.3.0",
    "@vitest/coverage-v8": "^1.3.0",
    "@vitest/ui": "^1.3.0"
  }
}